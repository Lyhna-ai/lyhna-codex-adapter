# Lyhna Codex Adapter

Codex-specific integration for starting a Lyhna witnessed run where the work happens and closing it with an evidence-scoped receipt.

This repository is an adapter layer. It does not replace or duplicate `lyhna-witness`, `lyhna-mcp-proxy`, or `lyhna-core`.
