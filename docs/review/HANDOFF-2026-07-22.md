# Session Handoff — Lyhna Codex Adapter Build Lead (2026-07-22)

> **Written 2026-07-22 by the build lead session (Claude) at Adam's direction, for the next session to take over with full fidelity.** Read this first for current state and the next build; then `HANDOFF-2026-07-16.md` for the role, the working relationship, and the constitution (it remains the umbrella — nothing here re-litigates it); then the reading order in `README.md`. The repo is the memory; everything referenced is committed here.

---

## 1. Role, unchanged — plus standing rules learned this window

The role sentence stands: *The builder produces. The evaluator examines. Lyhna witnesses. The corpus grades. Adam decides.* All §1/§4 constraints of the 2026-07-16 handoff remain binding (permission vocabulary banned; never-judge ≠ never-show; wrong `runtime_hook` stamp = evidence overclaim; field gates over CI; spec before code; builders never review their own work).

New standing rules, distilled from this window's field evidence (all recorded in `FIELD-EVIDENCE-2026-07-17.md`):

- **The witness is never the story, and the lead never makes it the assignment (CZ-13).** Witness-data extraction NEVER routes through Adam's working agents. The only witness ask of a working agent is `request_close` at the end of real work. Data moves by Adam zipping the local data root (`%USERPROFILE%\.lyhna\codex-adapter\`) and uploading it — proven fast and clean.
- **We are not involved in Adam's builds.** We write the witness and make it good; Adam points it at his work when he chooses. No participation in Homestead arcs, no asks of his agents, no deadlines on his runs.
- **The ledger is the truth; state is a cache of it.** Slice B's nine review rounds all reduced to this: idempotency keys and state overwrites must discriminate semantically distinct observations.
- **Closure is testimony until witnessed (CZ-14).** "The agent said it was done" is exactly as unverifiable as "the agent said it sent the email." This is the next build (§3).

## 2. Exact current state

| Thing | State |
|---|---|
| Working branch | `claude/lyhna-adapter-build-lead-ucebb6` (develop and push here; stacks on PR #2's branch, never modifies it) |
| PR #3 | https://github.com/Lyhna-ai/lyhna-codex-adapter/pull/3 — draft, base `claude/lyhna-adapter-review-thread-k1gpa3`; carries Slice B + this window's record. Re-subscribe to it on takeover (subscriptions die with the session). |
| Adapter version | **0.1.26**, code-final at `0ee68f3`; 63/63 tests; validator green; clean Codex verdict ("no major issues") after nine hosted review rounds — eight real findings fixed with fixtures, none declined — plus two field-driven fixes (stale-capability structural errors; refresh keying on evaluation activity) |
| Slice A | **CLOSED, field-proven.** Three `runtime_hook` confirmations (first-message structured link, mid-message after preamble, third on 0.1.26); four pending-miss markers all correct rejections. Zero false negatives, zero false positives. |
| Slice B | Code-complete and field-validated (`trigger` and `open_predecessors` observed live in the B11 run); **field gate NOT yet run** — it needs a sealed 0.1.26 packet handed cold to blind readers (acceptance: credit the seal, reconstruct head chains, no CZ-8/9/10-family corrections). The gate waits until Adam next uses the witness; nothing is owed and nobody is asked. |
| Installed on Adam's machine | 0.1.26 from this branch (pre field-fix commits — those live in error/edge paths; suitable as-is). Install procedure: update `C:\dev\lyhna-codex-adapter` checkout to this branch → `codex plugin add lyhna-codex-adapter@lyhna-ai` → full Codex restart. |
| Baseline packet | `field-packets/2026-07-17-run_de214e61-v0.1.25/` — sealed 1,161-event run, verified by the 0.1.26 verifier through the legacy path (the backward-compat fix proven on real data). The "before" exhibit for the blind readers. |
| Field ledger | `FIELD-EVIDENCE-2026-07-17.md` — §§1–11 plus 07-22 addenda: CZ-13 (and its escalation), CZ-14 candidate, first 0.1.26 run, the witness's first two credited code catches (a divide-by-zero the project suite missed; a ledger contradiction). |
| PR #1 / PR #2 | Unchanged, open, Adam's decisions. Never push to their branches. |
| Known open runs | Three unsealed runs exist in Adam's data root (by narrator non-close — the CZ-14 motivation). Leave them alone; CZ-12 surfaces them honestly. |

## 3. The next build: CZ-14 — seal-as-you-go (Adam's decision, 2026-07-22)

Adam chose checkpoint anchors over an operator-close command ("that's a good receipt and something everyone wants to know: did this close or is it still open — did they just tell me it was closed"). The operator-close command is **dropped as unnecessary**. Spec:

1. **Checkpoint anchor at every Stop.** In `checkpointOrSeal`'s non-sealing paths (plain checkpoint AND close-deferred), after the event is appended: render `receipt.json`/`RECEIPT.md` for the current state and write `checkpoint-anchor.json` (same discipline as the seal anchor: run id, `as_of_seq`, ledger tip, state hash, receipt hashes, `receipt_renderer`). Latest checkpoint overwrites the prior one; history lives in the ledger.
2. **Lifecycle status on the receipt face**, observational vocabulary only, one of: `SEALED`; "A close was requested at event X; the run had not sealed as of this checkpoint" (render the latest `close_deferred` blockers as observations); "OPEN as of event N — no close request observed." Never "abandoned," never inferred completion.
3. **Open-packet verification.** Extend the verify/read path: for an unsealed run, verify the hash chain to `as_of_seq` plus files-match-checkpoint-anchor, renderer-version-gated exactly like seals (ledger-pinned, not anchor-pinned — reuse the round-1 lesson). Structural results, never raw Node errors.
4. **Constraints, all standing:** deterministic (no clock/randomness/model calls); zero new dependencies; additive schema only; legacy runs (no checkpoint anchor) read exactly as before; no permission vocabulary; version bump to **0.1.27** via the shared constant (three surfaces, validator enforces); every change lands with fixtures — including the B11 shape (close requested, seal deferred by blockers, run left open: face must state it) and tamper-on-open-packet (must fail closed).
5. **Loop, exactly as proven:** Opus builder subagent → independent adversarial reviewer subagent (brief it on the honesty ceiling, determinism-vs-anchor, vocabulary, privacy, legacy compat) → lead consolidated fix round → push to PR #3 → `@codex review`, thread-by-thread with a fix commit and reply per finding (decline with written rationale when wrong) until clean → record the build outcome in `BUILD-PLAN-2026-07-16.md` in the established format.
6. **After clean:** tell Adam 0.1.27 is ready to install at his convenience. Then wait. The Slice B gate runs on whatever run seals naturally when he next uses the witness; with CZ-14 landed, even his three open runs become verifiable packets at their last checkpoints.

## 4. Continuation prompt for the next window

> You are taking over as build lead for the Lyhna Codex Adapter. Read `docs/review/HANDOFF-2026-07-22.md` on branch `claude/lyhna-adapter-build-lead-ucebb6` in Lyhna-ai/lyhna-codex-adapter first — it is the current state and carries the next build's spec; then `HANDOFF-2026-07-16.md` for the role and constitution; then the reading order in `docs/review/README.md`. Develop on `claude/lyhna-adapter-build-lead-ucebb6`; PR #3 is the working PR — re-subscribe to it. The next build is CZ-14 (seal-as-you-go checkpoint anchors + lifecycle status on the receipt face), spec in handoff §3 — run the cross-platform loop exactly as described. Do not touch Adam's other repositories, do not route witness errands through his working agents, and do not ask anything of his runs. The Slice B field gate runs when Adam next uses the witness. Adam decides all merges, scope, Thesis v2 adoption, and Forks A–D.

*The builder produces. The evaluator examines. Lyhna witnesses. The corpus grades. Adam decides.*
