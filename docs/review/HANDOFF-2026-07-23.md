# Session Handoff — Lyhna Codex Adapter Build Lead (2026-07-23)

> **Written 2026-07-23 by the build lead session (Claude) at Adam's direction, for the next session to take over with full fidelity.** Read this first for current state and what comes next; then `HANDOFF-2026-07-16.md` for the role, the working relationship, and the constitution (it remains the umbrella — nothing here re-litigates it); `HANDOFF-2026-07-22.md` is now historical (its §3 CZ-14 spec shipped). Then the reading order in `README.md`. The repo is the memory; everything referenced is committed here.

---

## 1. Role and standing rules — unchanged

The role sentence stands: *The builder produces. The evaluator examines. Lyhna witnesses. The corpus grades. Adam decides.* All constraints of the 2026-07-16 handoff remain binding (permission vocabulary banned from schemas and surfaces; never-judge ≠ never-show; a wrong `runtime_hook` stamp is an evidence overclaim; field gates over CI; spec before code; builders never review their own work). The standing rules distilled in the last two windows all carry forward:

- **The witness is never the story, and the lead never makes it the assignment (CZ-13).** Witness-data extraction NEVER routes through Adam's working agents. The only witness ask of a working agent is `request_close` at the end of real work. Data moves by Adam zipping the local data root (`%USERPROFILE%\.lyhna\codex-adapter\`) and uploading it.
- **We are not involved in Adam's builds.** We write the witness and make it good; Adam points it at his work when he chooses. No participation in his build arcs, no asks of his agents, no deadlines on his runs.
- **The ledger is the truth; state is a cache of it.** Every review round of Slice B and CZ-14 reduced to this: idempotency keys and state overwrites must discriminate semantically distinct observations, and no mutable file may ever select a weaker verification path than the hash chain commits to.
- **On any resume, read the tree first.** The first act of a takeover is to read the working tree and the PR/branch state directly — never to wait on a signal that may not arrive. (The 20-hour idle lesson, recorded in the BUILD-PLAN; it is the exact failure this product exists to expose.)

## 2. Exact current state

| Thing | State |
|---|---|
| Default branch `main` | **Carries everything.** All three working PRs merged 2026-07-23: PR #1 (witnessed-run v0), PR #2 (evaluator record + Slice A/B), PR #3 (CZ-14). Merge commits `d8cedd5` (#1) → `450ae49` (#2, into main) and `9d7f2bb` (#3, into #2's branch). `main` tip after the stack merge is `450ae49`. |
| Adapter version | **0.1.27** on `main` — CZ-14 code-final and Codex-clean at `2a6b384`; 99 tests across the suite (36 in `cz14.test.mjs`, including the full crash-consistency matrix); validator green. |
| Working branches (merged) | `codex/witnessed-run-v0`, `claude/lyhna-adapter-review-thread-k1gpa3`, `claude/lyhna-adapter-build-lead-ucebb6` are all merged into `main`. Do not build on them again — **branch fresh from `main`** for any new work. |
| This handoff's branch | `claude/cz-14-handoff-plan-592kpt` — carries only this closeout (handoff doc + README/BUILD-PLAN close notes). Its PR is a docs-only wrap-up, not a build. |
| Slice A | **CLOSED, field-proven.** Three `runtime_hook` confirmations, four pending-miss markers all correct rejections. Zero false negatives, zero false positives. |
| Slice B | **Code-final, field gate still owed.** The blind-reader gate needs a sealed 0.1.27 packet handed cold to blind readers (acceptance: credit the seal, reconstruct head chains, no CZ-8/9/10-family corrections). It runs on whatever run seals naturally when Adam next uses the witness. Nothing is owed by Adam and nobody is asked. |
| CZ-14 (seal-as-you-go) | **Code-final, field gate still owed.** Checkpoint anchor at every Stop; three-shape lifecycle face (`SEALED` / close-requested-not-sealed with blockers / `OPEN as of event N`); open-packet verification with full hash-chain discipline. Seventeen hosted review rounds beyond the adversarial pass, every finding fixed with a fixture, none declined. Gate: a cold **open** packet verifies and states its own lifecycle honestly, and the four stuck-open field runs become verifiable packets at their last checkpoints. |
| Installed on Adam's machine | 0.1.26 (previous). **0.1.27 is ready to install** at his convenience: update the `C:\dev\lyhna-codex-adapter` checkout to `main` → `codex plugin add lyhna-codex-adapter@lyhna-ai` → full Codex restart. Not urgent; his call. |
| Field ledger | `FIELD-EVIDENCE-2026-07-17.md` — §§1–11 plus the 07-22 addenda (CZ-13 and its escalation, the CZ-14 candidate, the first 0.1.26 run, the B11 two-sided closeout that is the CZ-14 thesis observed verbatim). |
| Known open runs | Four unsealed runs in Adam's data root (narrator non-close — the CZ-14 motivation). Leave them alone; with 0.1.27 installed they read as honest open packets at their last checkpoints. |

## 3. What comes next — the field gate, then Slice C

The standing rule holds: **one live edge at a time; a slice ships only when the one below passed its field gate.** So the next build (Slice C) does **not** start yet. The immediate forward work, in order:

1. **The field gate that's owed (Slice B + CZ-14), and it's Adam-paced.** It runs when Adam next uses the witness on real work and a run seals (Slice B) or is left open (CZ-14). When a packet exists: hand it cold to blind readers per the gate criteria above, record the read in `FIELD-EVIDENCE`, and mark the gate passed or capture the corrections. Do not manufacture a run to force the gate; do not ask Adam's agents for one.
2. **Only after Slice B's gate passes: Slice C — Full Vision (stop discarding the fuel).** Roadmap Step 1 + CZ-5 + CZ-6: rich Tier-0 local capture (payloads, claims, evaluator findings — readable), hash-bound to the sealed receipt; tiered projection at every export boundary (validator-enforced); structural-identity fields on every consequential event; the value-free sensitive-output observation. Field gate: a receipt reads end-to-end for human and agent; structural fields on 100% of consequential events; zero raw content in boundary exports. Full slice ladder (C→H) with per-slice field gates is in `BUILD-PLAN-2026-07-16.md` §2.
3. **The loop is unchanged when a build does start:** spec before code → Opus builder subagent → independent adversarial reviewer subagent → lead consolidated fix round → push → `@codex review` thread-by-thread until clean (decline with written rationale when a finding is wrong) → record the outcome in the BUILD-PLAN in the established format → field gate decides done.

Everything above the current edge is still Adam's decision: Thesis v2 adoption, Forks A–D, scope, and all merges.

## 4. Continuation prompt for the next window

> You are taking over as build lead for the Lyhna Codex Adapter. On `main` in Lyhna-ai/lyhna-codex-adapter, read `docs/review/HANDOFF-2026-07-23.md` first — it is the current shipped state; then `HANDOFF-2026-07-16.md` for the role and constitution; then the reading order in `docs/review/README.md`. The full stack is merged: `main` is at v0.1.27 with Slice A (field-proven), Slice B (code-final), and CZ-14 (code-final, Codex-clean). Branch fresh from `main` for any new work — the old working branches are merged and closed. There is no build in flight. The immediate forward work is the Slice B + CZ-14 field gate, which is Adam-paced: it runs when he next uses the witness and a packet exists, at which point you hand it cold to blind readers and record the read. Slice C does not start until Slice B's gate passes (one live edge at a time). Do not touch Adam's other repositories, do not route witness errands through his working agents, and do not ask anything of his runs. On resume, read the tree and PR/branch state directly before anything else. Adam decides all merges, scope, Thesis v2 adoption, and Forks A–D.

*The builder produces. The evaluator examines. Lyhna witnesses. The corpus grades. Adam decides.*
