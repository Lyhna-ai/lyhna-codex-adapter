# Session Handoff — Lyhna Codex Adapter Build Lead (2026-07-18)

> **Written 2026-07-18 by the incoming lead session (Claude), superseding [HANDOFF-2026-07-16.md](HANDOFF-2026-07-16.md).** The 07-16 handoff was written while the project was on hold awaiting the Slice A field gate; a full build window (2026-07-17, branch `claude/lyhna-adapter-build-lead-ucebb6`, PR #3) ran after it — Slice A's gate passed, Slice B was built and reviewed clean, and new field findings landed. This document is the umbrella now: read it first, then the reading order in `README.md`. Everything referenced here is committed in this directory or visible on the PRs — no prior conversation is required.

---

## 1. Your role and the settled principles (do not re-litigate)

You are the **build lead** for the lyhna-codex-adapter. Lineage: independent third-party evaluator (PR #1 review, F1–F6) → product strategist (Thesis v2 + dossier) → customer-zero reviewer (CZ-1–CZ-12) → build lead (Slice A shipped and field-proven; Slice B shipped through the loop). Adam decides; you lead the build, spec before code, and review everything.

The standing role sentence, used everywhere: *The builder produces. The evaluator examines. Lyhna witnesses. The corpus grades. Adam decides.*

Settled principles carried forward from the 07-16 handoff, all still binding:

- **The dual feed is the settled value thesis** — the receipt serves human AND agent readers; the agent may be the first-class reader.
- **"If it can be more, let's be more."** Stopping conditions are failed gates, not step counts.
- **Permission vocabulary is banned** (approve/escalate/refuse/block) from schemas and surfaces. Observational past-tense only.
- **Never-judge ≠ never-show.** The honesty ceiling (never certify) never changes; capture rich, project down at export boundaries by tier.
- **A wrong `runtime_hook` stamp is an evidence overclaim** — false positives are not free at the evidence layer. Field record now proves the matcher holds this on both sides (§2).
- **Field gates over CI.** A slice is done when it passes on Adam's real builds.

New principles distilled in the 2026-07-17 window, now equally standing:

- **Idempotency keys and state overwrites must discriminate semantically distinct observations — the ledger is the truth, state is a cache of it.** (The theme behind all six Codex rounds on Slice B.)
- **Rung 3 of the semantic ladder is constitutionally constant** — "Not established by this record" — until a structural clean-evaluation signal exists. `RECORDED` attests checkout integrity only.
- **Open-run vocabulary is observational, never "abandoned"** — a run left `OPEN` can be resumed (`begin_run` re-attaches idempotently); the record states what was observed at a point in the ledger, nothing more (CZ-12 amendment, `FIELD-EVIDENCE-2026-07-17.md` §2).
- **The witness must never become the story (CZ-13) — and the lead must never make it the assignment.** Witness narration belongs only at run open, run close, and `NEEDS_DECISION`; witnessing never appears in a plan as a work item beyond begin/read/close. **Witness-data extraction never routes through working agents** — the only witness ask of a working agent is `request_close` at the end of real work; the operator moves data by zipping the local data root (proven in the field: faster and cleaner than agent-courier round trips). (`FIELD-EVIDENCE-2026-07-17.md` §5, §11.)

## 2. Exact current state (2026-07-18)

| Thing | State |
|---|---|
| Latest work branch | `claude/lyhna-adapter-build-lead-ucebb6` at `3957dc4` (all 2026-07-17 work committed and pushed) |
| This handoff's branch | `claude/handoff-docs-sequencing-5bohsc`, stacked on the branch above; docs only |
| PR #1 | https://github.com/Lyhna-ai/lyhna-codex-adapter/pull/1 — adapter v0 (`codex/witnessed-run-v0` at `013c70f`), open, unmerged, Adam's decision. Never push to it. |
| PR #2 | https://github.com/Lyhna-ai/lyhna-codex-adapter/pull/2 — draft; evaluator record + Slice A (`claude/lyhna-adapter-review-thread-k1gpa3` at `835318b`), stacked on PR #1's branch |
| PR #3 | https://github.com/Lyhna-ai/lyhna-codex-adapter/pull/3 — draft; Slice B (`claude/lyhna-adapter-build-lead-ucebb6`), stacked on PR #2's branch |
| Adapter version | **0.1.26** code-final at `c837584`; 60/60 tests (37 inherited + 23 new fixtures); validator green; CI green; hosted Codex round-7 verdict: no major issues |
| Install state | **v0.1.26 installed and confirmed on Adam's Windows machine (2026-07-18, operator-reported), not yet used** — no run has been made on it. The first real run in a fresh Codex task at 0.1.26 is the Slice B gate run. |
| Slice A | **Field gate PASSED (2026-07-17), twice**: `run_5c99171b…` (first-message markdown plugin-link mention) and `run_de214e61…` (mid-message mention after preamble — the exact CZ-1 shape that defeated v0). Four pending-miss markers all correct rejections. Field record: zero false negatives, zero false positives. |
| Slice B | Code-complete, clean cross-platform review, **awaiting field gate** (blind-reader test, §4 below) |
| Baseline packet | Sealed 0.1.25 run banked at `field-packets/2026-07-17-run_de214e61-v0.1.25/` (1,161 events; verified `ALREADY_SEALED` by the 0.1.26 verifier through the legacy path; tamper check held). **This is the "before" exhibit for the Slice B gate** — it shows the CZ-3/CZ-8 ambiguity live. |
| Thesis v2 | Draft, adoption still Adam's open decision |
| Forks A–D | All still open, all Adam's |
| PR watching | Prior sessions' subscriptions died with them. Re-subscribe to PR #3 (and #2) when active work resumes. |

Open loose ends (small, tracked, none blocking):

- The build-plan Slice A gate record still awaits `invocation.matched_form` from `run_5c99171b…`'s sealed receipt (that run's thread continued; append when its sealed record is supplied). Note: the 0.1.25 receipt export carries `objective_origin` but no receipt-level `invocation` block — matched form lives on the run record.
- CZ-13 narration-budget correction is skill-text only, foldable into the next skill touch (Slice C).
- Abandoned atomic-write temp files graduated from footnote to a small future work item (`FIELD-EVIDENCE-2026-07-17.md` §9).
- The one-gesture packet work item (human-first entry point on the packet's face; one-step export/share) is recorded for Slice C scope (§11 of the field-evidence note).

## 3. The operating loop (proven twice now — Slices A and B)

1. **Spec first** with acceptance criteria (`SLICE-B-SPEC-2026-07-16.md` is the standard).
2. **Opus builder subagent** implements; builders never review their own work.
3. **Independent adversarial reviewer subagent** attacks the diff (Slice A: 6 real bypasses; Slice B: one P0, one P1, six more real findings).
4. **Lead reviews everything**, applies the consolidated fix round.
5. **Push to the working PR, comment `@codex review`** — the cross-platform brain check. Per the operator's 2026-07-17 loop-mechanics directive: push at each appropriate step, request review promptly at each checkpoint, and **re-ping a requested review on silence** rather than waiting indefinitely. Reply to each thread with the fix commit, resolve, re-request until clean. Judgment is allowed: decline with written rationale when a finding is wrong (Slice A declined one; Slice B declined none).
6. **Field test on Adam's real builds** — the gate that decides done.

## 4. The single next external event: the Slice B field gate

Everything is staged; nothing builds until this gate runs. The sequence, honoring the extraction rule in §1:

1. **Adam opens a fresh Codex task on the installed 0.1.26** (hooks bind at session start; an old thread keeps the old plugin). Install done and confirmed 2026-07-18.
2. **One real run** on a real goal. The only witness ask of the working agent is `request_close` at the end. Before requesting close, refresh any PR snapshot whose head has moved, so superseded evaluations resolve to `STALE` instead of blocking the seal (field finding, `FIELD-EVIDENCE-2026-07-17.md` §9). The seal fires on the real `Stop` hook, after the agent's final message — the sealed record's first possible reader is the next turn or session (§8); do not ask a run to narrate its own seal.
3. **Adam zips the local data root** and supplies it. No agent-courier errands.
4. **Lead verifies the seal** with the verifier, banks the 0.1.26 packet beside the 0.1.25 baseline, and runs the gate: **blind readers with zero Lyhna context must credit the seal and reconstruct the per-PR head chains from the packet alone.** The banked 0.1.25 packet is the before-exhibit for the comparison.
5. **On pass:** record the gate in the build plan, fold the CZ-13 narration budget into the Slice C skill touch, and spec Slice C (roadmap Step 1 + CZ-5/CZ-6 + the one-gesture packet item). **On miss:** fix from evidence, one commit, re-verify, re-gate — never from guesswork.

Do not start Slice C's code before the Slice B gate unless Adam explicitly says to parallelize.

## 5. The PRD, by reference (canonical — do not duplicate)

- **`BUILD-PLAN-2026-07-16.md`** — slices A–H with field gates, the loop, standing rules, and the build records for Slices A and B (including both gate records). **The master plan.**
- **`FIELD-EVIDENCE-2026-07-17.md`** — the newest evidence: both Slice A gate passes, the CZ-12 amendment, the operator's field problem statement (the derailed plan-mode session — Thesis v2 restating itself at scale), the learning-artifact angle and its honesty boundaries, CZ-13 and its escalation, the seal-at-Stop and fail-closed-close field verifications, and the banked baseline packet.
- **`SLICE-B-SPEC-2026-07-16.md`** — Slice B's spec (built; CZ-11/CZ-12 folded in 2026-07-17).
- **`CUSTOMER-ZERO-REVIEW-2026-07-16.md`** — findings ledger CZ-1–CZ-12, the evidence behind every slice.
- **`FULL-VISION-ROADMAP-2026-07-16.md`** — the seven-step reclamation ladder with the never-judge/never-show distinction and the Step 5 kill-guard.
- **`THESIS-V2.md`** + **`LYHNA-REVIEW-DOSSIER.md`** — strategy layer, preserved verbatim; adoption is Adam's.

## 6. Practical conventions (follow exactly)

- Commit style: imperative subject, body explaining what/why, both trailers (`Co-Authored-By` + the session's `Claude-Session:` URL). Never put model IDs in commits/PRs.
- All strategy/evidence writing goes in `docs/review/` and is committed immediately — the repo is the memory.
- Version bumps move all three surfaces via the shared `version.mjs` constant (validator enforces).
- Zero new dependencies; deterministic code only; match existing idiom; every fix lands with fixtures.
- Branches stack, never modify their base: PR #1's branch is untouchable; each session works on its own branch atop the latest. Merges are Adam's.
- Be frugal with GitHub comments; review-thread replies state the fix commit in 1–3 sentences, or decline with reasoning.

## 7. Continuation prompt for the next window

> You are taking over as build lead for the Lyhna Codex Adapter. Read `docs/review/HANDOFF-2026-07-18.md` on the latest branch of Lyhna-ai/lyhna-codex-adapter first — it is the umbrella; then follow the reading order in `docs/review/README.md`. Slice A's field gate passed; Slice B is code-final at v0.1.26 on PR #3 and awaits its field gate (install 0.1.26 fresh, one real run sealed and closed, packet to blind readers — §4 of the handoff). Do not re-litigate settled principles (§1); do not start Slice C's code before the gate unless Adam says to parallelize; run the loop exactly as §3 describes, and never route witness-data extraction through working agents. Adam decides all merges, Thesis v2 adoption, and Forks A–D.

*The builder produces. The evaluator examines. Lyhna witnesses. The corpus grades. Adam decides.*
