# Session Handoff — Lyhna Codex Adapter Build Lead

> **SUPERSEDED (2026-07-18).** The state described below is one build window out of date: the 2026-07-17 session (branch `claude/lyhna-adapter-build-lead-ucebb6`, PR #3) passed the Slice A field gate and shipped Slice B at v0.1.26. Read **[HANDOFF-2026-07-18.md](HANDOFF-2026-07-18.md)** instead — this document is preserved verbatim below as the record of the 2026-07-16 state.

> **Written 2026-07-16 by the lead session (Claude) at Adam's direction, for the next session to take over with full fidelity.** This is the umbrella document: read it first, then the reading order in `README.md`. Everything referenced here is committed in this directory or visible on the PRs — no prior conversation is required.

---

## 1. Your role and the working relationship

You are the **build lead** for the lyhna-codex-adapter, continuing a lineage that ran: independent third-party evaluator (PR #1 review, F1–F6) → product strategist (Thesis v2 + dossier, adversarially tested) → customer-zero reviewer (CZ-1–CZ-12) → build lead (Slice A shipped through the cross-platform loop). Adam decides; you lead the build, spec before code, and review everything.

The standing role sentence, used everywhere: *The builder produces. The evaluator examines. Lyhna witnesses. The corpus grades. Adam decides.*

Things Adam has made explicit that you must not lose:
- **The dual feed is the settled value thesis:** the receipt serves human AND agent readers. Field evidence (Part F of the customer-zero review) showed the agent may be the first-class reader.
- **"If it can be more, let's be more."** Stopping conditions are failed gates, not step counts. The full-vision roadmap runs to Step 7 (decision observability); nothing in the record argues for less.
- **Permission vocabulary is banned** (approve/escalate/refuse/block) from schemas and surfaces — it cost the project 60–90 days once (bind/authority era). Observational past-tense only (`PROCEEDED`/`HELD`/`TURNED_BACK`/`GATE_STOPPED`/`REDONE`). The guard is written into the roadmap's Step 3.
- **Never-judge ≠ never-show.** The honesty ceiling (never certify) is the moat and never changes. Content-blindness was authority's residue: capture rich, project down at export boundaries by tier. See the roadmap §0.
- **A wrong `runtime_hook` stamp is an evidence overclaim** — false positives are not free at the evidence layer of a witness product. Distilled during the Slice A review loop; treat as constitutional.
- **Field gates over CI.** Green checks have lied to this project before (F2). A slice is done when it passes on Adam's real builds.

## 2. Exact current state (2026-07-16, end of session)

**Project status: ON HOLD** — Adam has no active build to field-test on. Everything is staged for a one-message resume.

| Thing | State |
|---|---|
| Working branch | `claude/lyhna-adapter-review-thread-k1gpa3` (this branch; all work committed and pushed) |
| PR #2 | https://github.com/Lyhna-ai/lyhna-codex-adapter/pull/2 — draft, open, base `codex/witnessed-run-v0`; carries this record + Slice A code |
| PR #1 | https://github.com/Lyhna-ai/lyhna-codex-adapter/pull/1 — the adapter v0 (`codex/witnessed-run-v0` at `013c70f`), open, unmerged, awaiting Adam's decision. Never push to it. |
| Adapter version | **0.1.25** at `cf260f0` (code-final for Slice A); 37/37 tests; validator green |
| Install state | **v0.1.25 installed and verified on Adam's Windows machine** (all 10 tools registered, isolated smoke passed, cache at `C:\Users\Adam\.codex\plugins\cache\lyhna-ai\lyhna-codex-adapter\0.1.25`) |
| Slice A | Code-complete, clean final Codex review, **awaiting field gate** |
| Slice B | Fully specced (`SLICE-B-SPEC-2026-07-16.md`), not started — builds when A passes |
| Thesis v2 | Draft, **adoption still Adam's open decision**, preserved verbatim in this directory |
| Forks A–D (Thesis v2 §16) | All still open, all Adam's |
| PR watching | The prior session's webhook subscription and timers die with it. If active work resumes, re-subscribe to PR #2. |

**The Slice A field gate (the single next external event):** Adam opens a **fresh Codex task** (hooks load at session start — an old thread keeps the old plugin), invokes naturally (`@lyhna` or `@lyhna-codex-adapter`, anywhere in the message, preamble fine), runs a real goal. Pass = receipt shows `objective_origin: runtime_hook`, `mode: full`, and `invocation.matched_form` on the run record. Miss = collect the masked shapes from `pending-miss/` under the Lyhna data root and fix from evidence, not guesswork. A dump Adam already provided from the *old* thread (all `agent_reported`) is analyzed in Part H — do not mistake old-thread records for the field test.

## 3. What was built and how (the loop that must continue)

Slice A (CZ-1 front door + CZ-2 run-type rule) shipped through the full cross-platform loop, which is now the proven operating procedure for every slice:

1. **Spec first** with acceptance criteria (see `SLICE-B-SPEC-2026-07-16.md` for the standard).
2. **Opus builder subagent** implements; builders never review their own work.
3. **Independent adversarial reviewer subagent** attacks the diff (Slice A: found 6 real coverage bypasses).
4. **Lead reviews everything**, applies the consolidated fix round.
5. **Push to PR #2, comment `@codex review`** — hosted Codex is the cross-platform brain check. Slice A took 8 rounds: 7 real, progressively narrower findings (all fixed with fixtures) + 1 category-error suggestion **declined with written rationale** (the runtime privacy rule does not govern authored documents — see the resolved thread on THESIS-V2.md). Reply to each thread with the fix commit, resolve it, re-request review until clean. Judgment is allowed: decline with reasoning when the finding is wrong.
6. **Field test on Adam's real builds** via the installed plugin — the gate that decides done.

Full build record with all findings: the "Build record" block under Slice A in `BUILD-PLAN-2026-07-16.md`.

## 4. The PRD, by reference (do not duplicate — these are canonical)

- **`BUILD-PLAN-2026-07-16.md`** — slices A–H with field gates; the operating loop; standing rules. **This is the master plan.**
- **`SLICE-B-SPEC-2026-07-16.md`** — the next build: seal self-containment (CZ-8), per-PR head chains with exactly one `CURRENT` and fail-honest `NOT_REFRESHED` (CZ-3), the explicit four-rung semantic ladder with acceptance constitutionally unassertable (CZ-9), structural temporal coverage (CZ-4), evaluation trigger enums (CZ-10). Code anchors verified at `cf260f0`. **Scope decision pending for CZ-11/CZ-12** (rejected-claim traces, abandoned-run visibility — Part H of the customer-zero review): recommend folding both into Slice B's build as they are receipt-truth family; confirm with Adam or decide as lead.
- **`CUSTOMER-ZERO-REVIEW-2026-07-16.md`** — findings ledger CZ-1–CZ-12 (Parts A–H), the evidence behind every slice.
- **`FULL-VISION-ROADMAP-2026-07-16.md`** — the seven-step Build 6/7 reclamation ladder (rich capture → structural identity → dual axis → corpus → reducer → two readers → decision observability), with the never-judge/never-show distinction and the Step 5 kill-guard (measurement never gates — bind must not return).
- **`THESIS-V2.md`** + **`LYHNA-REVIEW-DOSSIER.md`** — strategy layer, preserved verbatim; Thesis v2 supersedes the June thesis only if Adam adopts it.

## 5. Sequencing from here (dependency order)

1. **Wait state:** nothing builds until Adam has a real build running. Do not start Slice B's code early unless Adam explicitly says to parallelize (he was offered this once and did not take it).
2. **On field-gate evidence:** if pass — record it in the build plan (Slice A gate: PASSED, with the receipt fields), then launch the Slice B builder per spec. If miss — the `pending-miss/` masked contexts are the work order; fix the matcher from evidence, one commit, re-verify, re-gate.
3. **Slice B → gate = repeat blind-reader test** on a sealed packet from a real run (readers with zero Lyhna context must credit the seal and reconstruct head chains).
4. **Then C (full vision / rich capture — roadmap Step 1 + CZ-5/6), D (dual axis + cockpit), E (corpus), F (reducer), G (two readers), H (observability wedge)** — each gated, one live edge at a time. The ladder is the destination; the rule sets the pace.

## 6. Practical conventions (follow exactly)

- Commit style: imperative subject, body explaining what/why, and both trailers (`Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>` + the session's `Claude-Session:` URL). Never put model IDs in commits/PRs.
- All strategy/evidence writing goes in `docs/review/` and gets committed immediately — this project has lost work to ephemeral threads before; the repo is the memory.
- Version bumps move all three surfaces via the shared `version.mjs` constant (validator enforces).
- Zero new dependencies; deterministic code only (no clock/randomness in the engine); match existing idiom; every fix lands with fixtures.
- Evaluator/builder separation is architectural: this branch stacks on PR #1's branch but never modifies it; merges are Adam's.
- Be frugal with GitHub comments; review-thread replies state the fix commit in 1–3 sentences, or decline with reasoning.

## 7. Continuation prompt for the next window

> You are taking over as build lead for the Lyhna Codex Adapter. Read `docs/review/HANDOFF-2026-07-16.md` in Lyhna-ai/lyhna-codex-adapter on branch `claude/lyhna-adapter-review-thread-k1gpa3` first — it is the umbrella; then follow the reading order in `docs/review/README.md`. The project is on hold awaiting the Slice A field gate (one real `@lyhna` invocation in a fresh Codex task at v0.1.25). Do not re-litigate settled decisions (§1 and §4 of the handoff); do not start Slice B's code before the gate unless Adam says to parallelize; when work resumes, run the cross-platform loop exactly as §3 describes. Adam decides all merges, Thesis v2 adoption, and Forks A–D.

*The builder produces. The evaluator examines. Lyhna witnesses. The corpus grades. Adam decides.*
